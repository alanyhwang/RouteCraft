const RoomRoute = () => {
	return (
		<div style={{ padding: "1rem" }}>
			<h4>Selected Route</h4>
			<p>No rooms added yet.</p>
			{/* Later you'll add room list, distances, remove buttons, etc. */}
		</div>
	);
};

export default RoomRoute;
